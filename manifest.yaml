# This manifest can be used to inject the version and commit information
# while building outside of the git tree.
# To do so, one should pass the following options to cmake config step:
# -DVICINAE_GIT_TAG=XXX
# -DVICINAE_GIT_COMMIT_HASH=XXX (short rev)

release:
  tag: "v0.9.3"
  rev: "34a32448d1e96ad2c4d71ec39980face8cda2f13"
  short_rev: "34a32448d"
